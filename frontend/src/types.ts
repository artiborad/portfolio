export type ExperienceItem = {
  company: string;
  period: string;
  role: string;
  highlight: string;
};

export type Project = {
  title: string;
  domain: string;
  description: string;
  techStack: string[];
  metric: string;
};
